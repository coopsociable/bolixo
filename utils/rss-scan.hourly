#!/bin/sh
LOG=/var/log/rssscan.log
date >>$LOG
if flock --close /var/run/bolixo-restart.lock rss-scan all >>$LOG 2>&1
then
	echo Succes rss-scan >>$LOG
else
	echo "***" Echec >>$LOG
fi
echo ======fin===== >>$LOG
date >>$LOG
