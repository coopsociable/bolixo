This directory contains the protocol definition. The files *.proto define the various protocols.

The files *.proto.doc contain documentation of the corresponding proto file.

The files *.protoh are generated and contain the server code.
The files *.protoch are generated and contain the client code.
The files *.protodef are generated and contain code for user-defined types used by clients.

See the file /usr/share/tlmp/build-protocol.doc for more information.
